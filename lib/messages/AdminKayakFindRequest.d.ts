export declare class AdminKayakFindRequest {
    usedBy: string;
    resourceId: string;
    sort: string;
    page: number;
    pageSize: number;
    constructor(usedBy?: string, resourceId?: string, sort?: string, page?: number, pageSize?: number);
}
